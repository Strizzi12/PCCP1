This is the first exercise of the lecture "Parallel Computing" at the University for Applied Sciences in Salzburg.

```ruby
require 'redcarpet'
markdown = Redcarpet.new("Hello World!")
puts markdown.to_html
```